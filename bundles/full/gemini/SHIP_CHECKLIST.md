# Ship Checklist

- Re-read the changed files and verify the diff still matches the request.
- Run the smallest credible verification set.
- Check external docs if commands, SDKs, or MCP behavior changed.
- State skipped checks and environment-dependent risk explicitly.
