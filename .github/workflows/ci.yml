name: ci

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install stable Rust
        run: |
          rustup toolchain install stable --profile minimal --no-self-update
          rustup default stable

      - name: Shell wrapper syntax
        run: |
          bash -n install.sh
          bash -n uninstall.sh
          bash -n install-release.sh

      - name: Release installer stdout smoke
        run: bash tools/smoke-release-installer-stdout.sh

      - name: Format
        run: cargo fmt --check

      - name: Help smoke
        run: |
          cargo run --quiet --bin stack-pilot -- --help
          cargo run --quiet --bin stack-pilot -- install --help
          cargo run --quiet --bin stack-pilot -- uninstall --help
          cargo run --quiet --bin stack-pilot -- doctor --help
          cargo run --quiet --bin stack-pilot -- wizard --help
          cargo run --quiet --bin sp -- --version

      - name: Clippy
        run: cargo clippy --locked --all-targets --all-features -- -D warnings

      - name: Tests
        run: cargo test --locked
