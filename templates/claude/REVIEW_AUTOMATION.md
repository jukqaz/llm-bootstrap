# Review Automation

Use this lane when repository workflow should enforce the same `review-gate`
contract that the local session uses.

## Contract

1. Define the PR or release gate as a markdown contract.
2. Register it in repository workflow, required checks, or branch protection.
3. Keep local task-state evidence and repo checks aligned.
